# Conv vs Transformer vs Mamba

## 一、核心对比
### 1. 卷积神经网络（ConvNet）
**核心思想**：以**空间局部性**和**权重共享**为核心归纳偏置，通过固定大小的卷积核在特征图上滑动提取局部特征，经多层堆叠逐步扩大感受野，形成“底层纹理-中层形状-高层语义”的层级化特征提取范式。现代卷积网络普遍引入残差连接解决深度退化问题。

- 代表论文1：《Very Deep Convolutional Networks for Large-Scale Image Recognition》（VGG）
  - 核心图示：**Table 1（架构配置表）** —— 展示6种深度的纯3×3卷积堆叠架构，直观体现卷积网络“统一小核、层级递进”的设计哲学，是卷积网络结构化设计的标杆。
- 代表论文2：《Deep Residual Learning for Image Recognition》（ResNet）
  - 核心图示：**Figure 2** —— 两种残差块结构（基础块与瓶颈块），通过shortcut恒等连接实现残差学习，解决了深层卷积网络的退化问题，是当前工业界最主流的卷积骨干形态。

### 2. Transformer
**核心思想**：完全摒弃卷积与循环结构，以**自注意力机制**为核心，通过动态计算序列中任意两个位置的query-key相似度，生成自适应的交互权重，实现全局范围的信息双向交互。无内置的局部性假设，完全靠数据学习依赖关系。

- 代表论文：《Attention Is All You Need》
  - 核心图示1：**Figure 1** —— Transformer整体编解码架构，展示“多头自注意力+前馈网络+残差归一化”的标准堆叠单元，奠定了所有Transformer变体的基础架构。
  - 核心图示2：**Figure 2** —— 左侧为缩放点积注意力的计算流程，右侧为多头注意力的并行结构，是自注意力机制的核心可视化表达。

### 3. Mamba
**核心思想**：基于**选择性状态空间模型（Selective SSM）**，通过隐状态的迭代更新实现序列建模；引入输入依赖的选择门控，动态控制信息的记忆与遗忘；配合硬件友好的并行扫描算法，在保持全局建模能力的同时，实现严格的线性时间复杂度。

- 代表论文：《Mamba: Linear-Time Sequence Modeling with Selective State Spaces》
  - 核心图示1：**Figure 3** —— Mamba核心模块结构，展示了从H3模块与门控MLP融合为Mamba块的设计逻辑，清晰呈现选择性SSM的数据流。
  - 核心图示2：**Figure 1** —— 不同架构在不同序列长度下的吞吐量对比曲线，直观体现Mamba线性复杂度在长序列场景下的压倒性优势。

## 二、多维度核心特性对比
| 对比维度 | ConvNet | Transformer | Mamba |
| :--- | :--- | :--- | :--- |
| **核心建模逻辑** | 局部滑动窗口+权重共享，层级扩大感受野 | 全局自注意力，动态计算任意位置相似度加权 | 状态空间迭代+选择性门控，线性扫描全局信息 |
| **显存空间复杂度** | 低，仅存特征图与卷积核 | 高，需存储完整注意力矩阵 | 低，仅存隐状态与中间特征 |
| **归纳偏置** | 强（局部性、平移不变性、层级性） | 弱，几乎无内置先验 | 弱，序列顺序先验，无空间局部先验 |
| **全局长距离建模** | 弱，感受野扩张缓慢，深层才有全局视野 | 极强，任意位置直接交互 | 强，可捕捉全序列依赖，接近Transformer |
| **局部细节建模** | 极强，天然适配纹理、边缘等底层特征 | 弱，高分辨率下局部细节易丢失 | 弱，纯序列建模缺乏局部空间约束 |
| **数据依赖程度** | 低，小数据集即可收敛 | 极高，需海量数据才能发挥优势 | 中等，数据效率优于Transformer、弱于CNN |
| **小样本泛化能力** | 强，归纳偏置提供强正则 | 弱，易过拟合 | 中等 |
| **训练并行性** | 高，卷积运算完全并行 | 高，自注意力可并行矩阵运算 | 高，硬件级并行扫描算法支持并行训练 |
| **长序列推理效率** | 高，线性复杂度 | 极低，平方复杂度爆炸 | 极高，线性复杂度+常数级显存 |
| **可解释性** | 中等，卷积核可可视化分析 | 较好，注意力权重可直接可视化 | 差，隐状态建模无直观可解释性 |
| **部署成熟度** | 极高，全硬件平台深度优化 | 较高，云端生态成熟，端侧仍在优化 | 低，依赖定制化CUDA内核，通用硬件适配不足 |

## 三、优劣势深度分析
### 1. ConvNet
**核心优势**
1. **数据效率极高**：强归纳偏置大幅降低学习难度，小数据集上表现远超另外两者，无需大规模预训练即可获得不错效果。
2. **参数效率高**：权重共享机制让卷积核参数量与输入尺寸无关，同等参数量下特征提取效率更高。
3. **部署生态最成熟**：CPU、GPU、FPGA、端侧芯片均有深度优化的算子库，落地成本最低，是工业界端侧部署的首选。
4. **局部特征提取能力强**：天然适配边缘、纹理、形状等底层视觉特征，对细节敏感的任务（如图像超分、医学影像分割）有天然优势。

**核心劣势**
1. **长距离建模能力薄弱**：感受野随深度线性增长，深层才能覆盖全局，且信息传递过程中易损耗，全局关系捕捉能力差。
2. **权重静态固定**：卷积核训练完成后权重固定，无法根据输入内容动态调整感受野与关注区域，建模灵活性不足。
3. **深度上限受限**：纯堆叠深度会出现网络退化，即使有残差连接，深度超过数百层后性能收益也急剧递减。

### 2. Transformer
**核心优势**
1. **全局建模能力天花板**：任意两个位置可直接计算注意力，信息传递无损耗，长距离依赖捕捉能力碾压卷积，是复杂语义建模的最优解。
2. **动态自适应建模**：注意力权重随输入内容动态变化，可自适应关注关键区域，对形变、尺度变化的鲁棒性更强。
3. **架构通用性极强**：同一套架构可无缝适配NLP、CV、音频、多模态等几乎所有任务，是当前大模型的通用基座。
4. **性能上限高**：在大数据、大算力的支撑下，性能随模型规模、数据量增长持续提升，scaling law表现最稳定。

**核心劣势**
1. **平方复杂度瓶颈**：自注意力计算量与显存占用随序列长度平方增长，高分辨率图像、长文本场景下算力成本极高，难以落地。
2. **数据效率极低**：缺乏内置归纳偏置，需要海量标注数据或大规模预训练才能收敛，小数据集上泛化性远不如CNN。
3. **落地成本高**：训练对算力要求极高，推理部署需大量优化工作，端侧设备难以运行大参数量Transformer模型。
4. **位置外推能力差**：绝对/相对位置编码对训练外的输入尺寸适配性差，输入分辨率变化易出现明显精度掉点。

### 3. Mamba
**核心优势**
1. **线性复杂度的全局建模**：同时具备“全局建模能力”与“线性时间/显存复杂度”，完美弥补了卷积全局弱、Transformer成本高的短板，序列越长优势越显著。
2. **推理效率极高**：推理阶段仅需维护隐状态，显存占用恒定，吞吐量远高于同精度Transformer，长序列场景下推理速度可提升数倍。
3. **选择性动态过滤**：输入依赖的门控机制可动态选择保留或遗忘信息，既能捕捉全局依赖，又能过滤冗余噪声，建模效率优于传统SSM。
4. **长序列场景降本增效显著**：支持百万级token上下文，无需复杂的滑动窗口、稀疏注意力等trick，架构简洁且效果稳定。

**核心劣势**
1. **视觉适配仍不成熟**：原生Mamba是一维序列建模，缺乏视觉任务的空间归纳偏置，纯Mamba骨干在中小分辨率、小数据集上表现弱于CNN与Transformer。
2. **部署门槛高**：性能依赖高度定制的硬件并行扫描内核，通用硬件、端侧芯片的适配工作尚未完善，工业落地生态远不如前两者。
3. **可解释性差**：状态空间的隐式建模无法像注意力一样直观可视化，模型行为难以分析与调试。
4. **技术迭代期不稳定**：作为新兴架构，视觉场景的最优设计（扫描方向、局部融合方式等）尚未形成统一标准，不同变体性能差异较大。
